# Polarity — frontend (Next.js)

Web UI and API routes: article URL or pasted text → Gemini panel analysis → reliability + left–right bias (Ad Fontes CSV blend).

From repo root: `npm run dev` (runs this package).  
From this folder: `npm run dev`, `npm run build`, `npm run start`.

**Deploy:** Vercel **Root Directory** = `frontend`.
