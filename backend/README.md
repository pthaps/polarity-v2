# Backend

This repository’s HTTP APIs are implemented as **Next.js Route Handlers** under [`frontend/src/app/api/`](../frontend/src/app/api/). Product and pipeline detail: [`docs/MASTER_PLAN.md`](../docs/MASTER_PLAN.md).

A separate backend service may be added here later (e.g. standalone Node or Python) if you split analysis or workers out of the Next app.
